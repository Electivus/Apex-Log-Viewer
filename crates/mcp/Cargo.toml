[package]
name = "apex-log-viewer-mcp"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
apex-log-viewer-cli = { path = "../cli" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
