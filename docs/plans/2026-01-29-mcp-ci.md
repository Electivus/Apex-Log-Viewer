# MCP CI Workflow Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a dedicated GitHub Actions workflow that builds and tests the MCP package when its files change.

**Architecture:** Create a new workflow `.github/workflows/mcp-ci.yml` scoped to MCP paths, running Node 22 with npm caching and standard build/test steps. Update `docs/CI.md` to document the new workflow.

**Tech Stack:** GitHub Actions, Node.js 22, npm.

### Task 1: Add MCP CI workflow

**Files:**
- Create: `.github/workflows/mcp-ci.yml`

**Step 1: Write the failing test**

No automated test for workflow structure; skip.

**Step 2: Create the workflow**

```yaml
name: MCP CI

on:
  push:
    paths:
      - '.github/**'
      - 'apps/sf-plugin-apex-log-viewer-mcp/**'
  pull_request:
    paths:
      - '.github/**'
      - 'apps/sf-plugin-apex-log-viewer-mcp/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build_and_test:
    name: Build and Test (MCP)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: apps/sf-plugin-apex-log-viewer-mcp
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js 22.x
        uses: actions/setup-node@v6
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: apps/sf-plugin-apex-log-viewer-mcp/package-lock.json

      - name: Install dependencies
        run: npm ci --workspaces=false

      - name: Build
        run: npm run build

      - name: Run tests
        run: npm test
```

**Step 3: Run tests to verify**

No local test command for workflow structure. Skip.

**Step 4: Commit**

```bash
git add .github/workflows/mcp-ci.yml
git commit -m "ci(mcp): add workflow"
```

### Task 2: Document the MCP CI workflow

**Files:**
- Modify: `docs/CI.md`

**Step 1: Update docs**

Add a bullet describing the MCP CI workflow, its triggers, and scope.

**Step 2: Commit**

```bash
git add docs/CI.md
git commit -m "docs(ci): add mcp workflow"
```
